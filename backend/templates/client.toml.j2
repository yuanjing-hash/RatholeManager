# Auto-generated by Rathole Manager
[client]
remote_addr = "{{ remote_server_addr }}:{{ transport_port | default(7000) }}"

{% for rule in services %}
[client.services.{{ rule.name | replace(' ', '_') }}]
type = "{{ rule.rule_type }}"
token = "{{ rule.token }}"
local_addr = "127.0.0.1:{{ rule.local_port }}"
{% endfor %}