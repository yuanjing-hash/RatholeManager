# Auto-generated by Rathole Manager
[server]
bind_addr = "0.0.0.0:{{ transport_port | default(7000) }}"

{% for rule in services %}
[server.services.{{ rule.name | replace(' ', '_') }}]
token = "{{ rule.token }}"
bind_addr = "0.0.0.0:{{ rule.remote_port }}"
{% endfor %}